# Created by https://www.toptal.com/developers/gitignore/api/macos,jekyll,zsh
# Edit at https://www.toptal.com/developers/gitignore?templates=macos,jekyll,zsh

### Jekyll ###
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
# Ignore folders generated by Bundler
.bundle/
vendor/

### macOS ###
# General
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon


# Thumbnails
._*

# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

### macOS Patch ###
# iCloud generated files
*.icloud

### Zsh ###
# Zsh compiled script + zrecompile backup
*.zwc
*.zwc.old

# Zsh completion-optimization dumpfile
*zcompdump*

# Zsh history
.zsh_history

# Zsh sessions
.zsh_sessions

# Zsh zcalc history
.zcalc_history

# A popular plugin manager's files
._zinit
.zinit_lstupd

# zdharma/zshelldoc tool's files
zsdoc/data

# robbyrussell/oh-my-zsh/plugins/per-directory-history plugin's files
# (when set-up to store the history in the local directory)
.directory_history

# MichaelAquilina/zsh-autoswitch-virtualenv plugin's files
# (for Zsh plugins using Python)
.venv

# Zunit tests' output
/tests/_output/*
!/tests/_output/.gitkeep

# End of https://www.toptal.com/developers/gitignore/api/macos,jekyll,zsh

# ——————————————————————————
# Private key and certificate files
# ——————————————————————————
# PEM, DER, PKCS#12, PFX, PKCS#8, etc.
*.pem
*.key
*.cer
*.crt
*.der
*.p12
*.pfx
*.pk8

# CSR / requests
*.csr

# ——————————————————————————
# SSH keys
# ——————————————————————————
# default ssh key names (uncomment any you use)
id_rsa
id_rsa*
id_dsa
id_dsa*
id_ecdsa
id_ecdsa*
id_ed25519
id_ed25519*

auth_id_rsa
auth_id_rsa*
auth_id_dsa
auth_id_dsa*
auth_id_ecdsa
auth_id_ecdsa*
auth_id_ed25519
auth_id_ed25519*

sign_id_rsa
sign_id_rsa*
sign_id_dsa
sign_id_dsa*
sign_id_ecdsa
sign_id_ecdsa*
sign_id_ed25519
sign_id_ed25519*

# ——————————————————————————
# Environment / credentials files
# ——————————————————————————
.env
.env.*           # e.g. .env.production, .env.local
credentials.json # gcloud / firebase
azure*.json      # Azure Service Principal creds
aws/credentials
aws/config

# ——————————————————————————
# Kubernetes configs
# ——————————————————————————
.kube/config
config.yaml     # helm, kustomize, etc. (if it holds creds)

# ——————————————————————————
# Other secret stores
# ——————————————————————————
*.vault
secrets.yml
*-private
*-secrets
*-private.xid

# Project specific ignores
.vscode/
.claude
untracked/
sandbox/
tmp/
*.tmp
*.bak
